/**
 * 路由相关的约束说明。
 *
 * ★ 前端路由的**完整路径**不能和后端任何一条路由的完整路径重合。
 *   nginx 把 {BASE}/* 全部转给后端，后端匹配不到业务路由才兜底发 SPA 外壳 ——
 *   重合的那条前端路由永远到不了浏览器，症状是「点那个菜单下载了一坨 JSON」。
 *
 *   注意比的是**完整路径**，不是首段：后端只有 /email/list、/email/send，
 *   没有 /email 本身，所以前端用 /email 当页面地址是安全的；
 *   但如果前端想要 /email/list 当页面地址，那就撞了。
 *
 *   scripts/check-route-collisions.mjs 在每次 build 前检查，
 *   后端路由表**直接从 FastAPI 应用读**，不手工维护。
 *
 * 已知的一次改名：旧前端 /info（关于我们）与后端 /info/* 撞车，
 * 前端改成 /about —— 用户可见的地址优先，接口地址是机器看的。
 */
export {};

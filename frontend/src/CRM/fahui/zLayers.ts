// 法会工作区的层级表。
//
// 手机端的订单抽屉是盖住整屏的一「页」（见 YlpDrawer），任何能从抽屉里再打开的
// 弹窗都必须压在它上面 —— 否则点了「没反应」：弹窗其实开了，只是躲在抽屉后面。
// 这坑踩过一次（新增付款弹窗停在 z-index 60），所以把数字集中到这里，
// 别再各写各的。

/** 手机端全屏抽屉 / 桌面右侧常驻栏 */
export const Z_DRAWER = 1200;

/** 从抽屉里打开的弹窗：新增付款、牌位编辑、付款凭证 */
export const Z_MODAL = 1300;

/** 还要压在弹窗之上的：公开链接二维码这类 */
export const Z_MODAL_TOP = 1400;

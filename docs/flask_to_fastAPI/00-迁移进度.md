# 迁移进度（活文档）

> 这份是**当前状态**，每搬完一批就更新。设计与方案看 01–15，这里只记「搬到哪了、还欠什么」。
>
> 最后更新：2026-09-23（批次二进行中）

## 一、总体

| 项 | 状态 |
|---|---|
| Web 框架 | ✅ FastAPI 接管 dev 站（5102）。**进程内 Flask 系模块 = 0** |
| 数据库 | ✅ PostgreSQL 16.15，alembic head `c3f8b21e7a06` |
| 目录结构 | ✅ 后端收进 `backend/`，模块改成包（`api/<模块>/router.py`） |
| 配置 | ✅ `system_config.env` + `core/config.py`（pydantic-settings） |
| 路径基座 | ✅ `BASE_PATH=/UTBA_DEMO`，nginx 已接 |
| 实时推送 | 🟡 SSE 层（`core/realtime.py`）已就绪，业务事件还在搬 |
| 生产 | ⬜ 仍跑 Flask：`gunicorn wsgi:app :5006`（`/srv/flaskapp/xinya`） |

## 二、模块迁移

**已搬 7 个**：`app_release` `camera` `email` `mobile` `permission_mgmt` `public_api` `twilio`

**未搬 24 个蓝图，共 522 条路由**（按规模降序）：

| 蓝图 | 挂载前缀 | 路由 | 备注 |
|---|---|---|---|
| form_bp | /form | 76 | 最大单体 |
| board_router_bp | /board_router | 61 | fahui/YLP |
| user_control_bp | /user_control | 48 | |
| event_data_bp | /event_data | 37 | 与 public_api 的 `/event_data/{id:int}` 同命名空间 |
| print_paiwei_bp | /print_paiwei | 30 | fahui/YLP |
| lamp_registration_bp | /lampRegistration_API | 30 | fahui/lamp |
| fahui_bp | /fahui_router | 27 | |
| asset_bp | /asset | 26 | |
| music_bp | /music | 26 | |
| files_bp | /files | 25 | |
| gl_bp | /gl | 18 | 批次二 |
| account_bp | /account | 16 | |
| diy_paiwei_bp | /diy_paiwei | 16 | fahui/YLP |
| fahui_payment_bp | /payment | 14 | |
| payment_bp | /payment | 13 | fahui/YLP |
| media_bp | /media | 9 | |
| quiz_game_bp | /quiz_game | 9 | 带 10 个 socket 事件 |
| info_bp | /info | 8 | 批次二 |
| changyou_room_bp | /changyou_room | 8 | 批次二，带 2 个 socket 事件 |
| quiz_bp | /quiz | 7 | 批次二，带 8 个 socket 事件 |
| songbook_bp | /songbook | 7 | 批次二 |
| payment_voucher_bp | /account/print_payment_voucher | 5 | |
| mirror_bp | /mirror | 5 | 带 9 个 socket 事件 |
| nginx_media_router | /media_file | 1 | |

## 三、跨模块待办（不属于任何单个模块，别漏）

### 🔴 会造成线上故障的

1. **nginx `/cctv_authz` 还指着 Flask**
   `/etc/nginx/conf.d/flaskapp_http.conf:132`
   ```
   proxy_pass http://127.0.0.1:5006/api/move_camera/authz;
   ```
   切到 FastAPI 后要改成新上游与路径（v3 是 `.../UTBA_DEMO/move_camera/authz`）。
   **改之前备份**，改完用 `curl -I` 验 200/401/403 三种情况。
   这条链路断了的症状是「登录了也看不到画面」或者「没登录也能看」——后者是安全事故。

2. **前端 402 处 `/api` 前缀要去掉**
   `/api` 段在 v3 里没有了（`BASE_PATH` 已经区分项目）。
   分布最密的几处：`CRM/event/react` `CRM/Account/react/claim` `js/` `CRM/form/react` `CRM/fahui`。
   已知明确点名的：`CRM/user_control/react/api.ts:104/109/119`、`profile/react/api.ts:55`。

### 🟡 功能性的

3. **22 个入向 Socket.IO 事件 → POST**（`backend/app/socket_events.py`）
   `quiz:*` 8 个、`changyou_*` 2 个、`mirror` 9 个、`quiz_game` 10 个、以及
   `join_room` / `parental_sign_sync` / `ping_test`。
   出向 `emit` 已随各模块改成 `publish_sync(...)`。

4. **前端 25 个 socket.io 文件 → EventSource**
   接口是 `{BASE}/{app}/realtime?room=...`。

5. **各模块的 `RealtimeApp` 规约要 register**
   `mirror` `quiz` `quiz_game` `changyou_room` `fahui board` 等，
   authorize / snapshot 回调见 `core/realtime.py` 的 `RealtimeApp` docstring。

### ⚪ 清理性的

6. **`env_value()` 兼容垫片**（`core/config.py` 末尾）
   给还没搬的模块用。**搬一个模块就把它那处改成直接读 `settings.xxx`**，全改完删掉本函数。
   当前还在用的：`quiz_game/ai.py`、`fahui/YLP/paiwei_ocr.py`、`event/services.py`、
   `account/read_bill_ark.py`、`account/services.py`、`form/ai_grouping.py`。

7. **`wsgi.py` + `backend/app/` + `_token.py` 删除**
   等生产切到 FastAPI。生产 systemd（`xinya_flask.service`）同时要改成 uvicorn。

8. **`docs/10-配置中心.md` §3 与实现不一致**
   文档写 `EXTENDS=`，实现是硬编码共享 env 路径（`/srv/flaskapp/system_config.shared.env`）。
   择一改。

## 四、开发约定

- 重启 dev 服务一律用 `./scripts/dev_server.sh restart`。
  **不要用 `pkill -f run.py`**——同机的 OCR_project / BearBad / sengchong 入口也叫 `run.py`，
  会被一起杀掉（已经误杀过一次，靠 systemd 自动拉起才没出事）。
- 搬模块的硬性约束（违反会启动即失败或线上事故）：
  1. 文件里不能写 `from __future__ import annotations`（core.auth 的装饰器用
     `functools.wraps` 包过，FastAPI 会去 `core/auth.py` 的命名空间求值注解，找不到 `Optional`）；
  2. 路由一律 `def` 不用 `async def`（底下是同步 DB/Redis 调用，会焊死事件循环）；
  3. 装饰器顺序：`@router.get(...)` 在上、`@login_required` 在下。

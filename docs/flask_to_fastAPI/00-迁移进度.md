# 迁移进度（活文档）

> 这份是**当前状态**，每搬完一批就更新。设计与方案看 01–15，这里只记「搬到哪了、还欠什么」。
>
> 最后更新：2026-09-23（批次二进行中）

## 一、总体

| 项 | 状态 |
|---|---|
| Web 框架 | ✅ **迁移完成**。536 条路由全部在 FastAPI 上，`backend/app/` 已删除 |
| Flask 残留 | ✅ 进程内 flask/werkzeug 模块 = **0** |
| 数据库 | ✅ PostgreSQL 16.15 |
| 目录结构 | ✅ `backend/{api,core,models,migrations}`，`main.py` 为入口 |
| 配置 | ✅ `system_config.env` + `core/config.py` |
| 路径基座 | ✅ `BASE_PATH=/UTBA_DEMO` |
| 前端 | 🟡 旧代码已全删（114,475 行），新架构搬迁中 |
| 实时推送 | 🟡 SSE 层就绪，`music` 已 register RealtimeApp；其余模块的出向已改 publish_sync |
| 生产 | ⬜ 未切换 |

## 二、模块迁移

**全部 31 个蓝图已搬完，536 条路由。**


# 06 · 逐阶段清单

执行时在这里打勾。`⬜` 未做 / `🟡` 进行中 / `✅` 完成。
形态是**一步到位 + 停机切换**，所以只有阶段 5 才真正上线；
但 **dev 域名（`yukang.utbabuddha.com`）从阶段 1 起每天都要能跑新栈**。

---

## 阶段 1 · 地基（2–3 周）

### 1.1 配置中心 → [10-配置中心.md](10-配置中心.md)

- [ ] `system_config.example.env`（带注释，入库）
- [ ] `core/config.py`（pydantic-settings，缺必填键即启动失败）
- [ ] 生产/dev 各建 `system_config.env`（chmod 600，加 .gitignore）
- [ ] 20 处配置读取点（14 `os.environ` + 6 `from _token import`）换成 `settings.*`
- [ ] 硬编码外部地址（BytePlus / Cloudflare / Resend / Google）改配置项
- [ ] `scripts/gen_frontend_env.sh`（白名单导出 + 密钥泄漏断言）
- [ ] 彻底删除 `_token.py` 与 `env_value()` 的 `.flaskenv` 读取

### 1.2 路径基座 → [11-BASE_PATH.md](11-BASE_PATH.md)

- [ ] `core/urls.py`：`base_path()` / `public_url()` / `absolute_url()`
- [ ] FastAPI `root_path` = `APP_BASE_PATH`
- [ ] 前端 `basePath.ts`；`apiFetch` 拼接加 `BASE_PATH`
- [ ] `vite.config.js` 的 `base`；**两份 index.html** 的资源路径
- [ ] 41 处 `window.location.origin + ...` → `publicUrl()`
- [ ] Cookie：`SESSION_COOKIE_NAME` 加项目前缀、`path` = base path
- [ ] nginx `location /UTBA_DEMO/`（dev）；旧路径**不必保留**，已确认
- [ ] nginx 加小写→规范写法的 301（`/utba_demo` → `/UTBA_DEMO`），见 [09](09-待决事项.md) D10

### 1.3 FastAPI 骨架

- [ ] `asgi.py`：FastAPI 实例、路由注册、异常处理器
- [ ] `ProxyHeadersMiddleware`（否则 `url_root` 变 127.0.0.1）
- [ ] CORS 用 `allow_origin_regex`（**不能 `["*"]` + credentials**）
- [ ] 请求体大小限制中间件（补回 `MAX_CONTENT_LENGTH`）
- [ ] `GET /healthz`（不鉴权不查库）
- [ ] `GET /api/time`（替代 socket 的时钟同步）
- [ ] 全局异常处理器：让 `HTTPException` 输出 `{"status":"error","message":...}` 形状

### 1.4 三层垫片 → [04-兼容层设计.md](04-兼容层设计.md)

- [ ] `core/db.py`：`db` 门面（Model/.query/session/get_or_404/类型透传）
- [ ] DB 会话中间件 + 按请求唯一的 `scopefunc`
- [ ] ★ **并发串场测试**（20 并发混合读写，校验无数据串场）—— 不通过不许继续
- [ ] `models/__init__.py` 切到 `core.db`（**模型文件本身不动**）
- [ ] `core/auth.py`：`current_user` 代理 + `login_required` + `permission_required`
- [ ] 会话 Cookie 读写（itsdangerous 同格式）
- [ ] ★ **用线上真实 Cookie 验证能认出同一用户**
- [ ] Bearer token 路径（复用 `session_service`）
- [ ] `core/responses.py`：`jsonify` 垫片 + 元组状态码的改写约定
- [ ] `render_template` 垫片（自动塞 request）

### 1.5 SSE 基础设施 → [12-SSE改造方案.md](12-SSE改造方案.md)

- [ ] `core/realtime.py`：Redis `psubscribe rt:*` 扇出 + 进程内房间表
- [ ] 统一端点 `GET /api/{app}/realtime`（多 `room=` 参数）
- [ ] `RealtimeApp` 注册表（authorize / snapshot 两个回调）
- [ ] `publish(app, room, event, data)` 发送端
- [ ] 心跳（15s `: keepalive`）与断连检测
- [ ] 队列满时丢消息而非阻塞
- [ ] nginx `location ~ /realtime$`：关缓冲、长超时
- [ ] ★ **nginx 开 HTTP/2**，DevTools 确认协议是 `h2`
- [ ] 前端 `realtime.ts`：`subscribeRealtime(app, rooms, handlers)`

### 1.6 APK 版本门禁（★ 切换前置，见 [03](03-迁移策略.md) §6）

- [ ] `GET /api/app/min_version` 接口
- [ ] APK 启动检查 + 全屏更新提示
- [ ] **提前 2–4 周发布过渡版并盯升级率**

---

## 阶段 2 · REST 重写（4–8 周）

顺序见 [05-模块迁移顺序.md](05-模块迁移顺序.md)。每个模块的标准动作：

```
- [ ] 路由改写（保持 def 同步；确认无阻塞才用 async）
- [ ] jsonify 元组状态码 → JSONResponse(status_code=)
- [ ] abort() → HTTPException（响应体从 HTML 变 JSON）
- [ ] 上传/表单 → UploadFile / Form
- [ ] send_file → FileResponse
- [ ] 该模块的 Flask 文件删除（不保留双份）
- [ ] dev 域名走查该模块的主要页面
```

- [ ] `permission_mgmt` (3) · [ ] `app_release` (2) · [ ] `public_api` (6) · [ ] `camera` (5)
- [ ] `twilio` (6) · [ ] `mobile` (6) · [ ] `quiz` (7) · [ ] `changyou_room` (8)
- [ ] `songbook` (7) · [ ] `content` (8) · [ ] `email` (5) · [ ] `mirror` (5)
- [ ] `quiz_game` (9) · [ ] `gl` (18) · [ ] `asset` (26) · [ ] `media` (10)
- [ ] `filesystem` (25) · [ ] `music` (26) · [ ] `account` (21) · [ ] `event` (37)
- [ ] `user_control` (48) · [ ] `form` (76)
- [ ] `fahui` 分四组：[ ] 订单牌位 · [ ] 打印 PDF · [ ] 点灯 · [ ] 付款与签名

**收尾**
- [ ] 路由数对账：新旧路由清单逐条比对，574 条一条不少（或明确记录哪条删了、为什么）
- [ ] `grep -rn "from flask" app models` 为空
- [ ] requirements 移除 Flask / Flask-Login / Flask-Cors / Flask-SQLAlchemy / Flask-Migrate
- [ ] alembic 脱离 Flask-Migrate 可独立运行（`migrations/env.py`、`alembic.ini`）
- [ ] 删除 `wsgi.py` / `socket_wsgi.py` / `socket_server.py` / `app/factory.py`

## 阶段 3 · 实时改造（2–3 周）

详见 [12-SSE改造方案.md](12-SSE改造方案.md) §3 的逐模块映射。

**后端：22 个动作改 POST**
- [ ] quiz：`tap`
- [ ] quiz_game：`answer` + 主持人 6 个动作
- [ ] mirror：`rate` + 主持人 4 个动作
- [ ] changyou_room：`push_song` + `join_room`
- [ ] form：`parental_sign_sync`
- [ ] 删除 `app/socket_events.py` 与各模块的 `socket_events.py`

**后端：23 种广播改 SSE**
- [ ] 8 个模块注册 `RealtimeApp`（authorize + snapshot）
- [ ] 16 处 `socket_broker.emit` → `realtime.publish`
- [ ] 后台线程（视频转码、牌位任务）改成写 Redis 而非直接 emit

**前端：21 个文件**
- [ ] `mediaRealtime.ts` · [ ] `CRM/fahui/socket.ts` · [ ] `CRM/form/*`（5 个文件）
- [ ] `changyou/room/socket.ts` · [ ] `quiz/*`（3 个） · [ ] `game/*`（2 个）
- [ ] `mirror/*`（2 个） · [ ] `form/react/*`（3 个） · [ ] 其余
- [ ] 删除 `socket.io-client` 依赖
- [ ] ★ 确认**一个页面只开一条 SSE**

**清理**
- [ ] 停用并删除 `xinya_socket.service`
- [ ] nginx 删除 `location /socket.io/`
- [ ] requirements 移除 `flask-socketio`、`eventlet`
- [ ] `grep -rn "socket.io\|socketio\|eventlet" app frontend/src` 为空

## 阶段 4 · 验收演练（1–2 周）

详见 [07-测试与验收.md](07-测试与验收.md)

- [ ] 功能走查清单逐项过（按模块，见 07 §3）
- [ ] 实时功能真机 + 弱网验收（见 [12](12-SSE改造方案.md) §7）
- [ ] 认证三条路：会话 Cookie / Bearer / 未登录 401
- [ ] 上传下载：图片、视频、PDF、APK
- [ ] 视频 Range 206 播放
- [ ] 并发压测：SSE 100 连接 + REST 混合，观察连接数与内存
- [ ] MySQL 连接数核对（worker × 线程池 vs `max_connections`）
- [ ] **停机演练**：在 dev 环境完整走一遍切换流程并计时
- [ ] 回滚演练：从新栈切回旧栈，确认 15 分钟内可完成

## 阶段 5 · 切换（1 天）

详见 [08-部署与回滚.md](08-部署与回滚.md)

- [ ] 确认 APK 升级率达标（门禁已生效）
- [ ] 公告停机窗口
- [ ] 数据库全量备份 ★
- [ ] 旧代码打 tag（`pre-fastapi`）
- [ ] 停机 → 部署新栈 → 冒烟 → 开放
- [ ] 观察 48 小时（错误日志、SSE 连接数、内存、MySQL 连接）
- [ ] 一周后删除旧 systemd unit 与旧代码目录备份
